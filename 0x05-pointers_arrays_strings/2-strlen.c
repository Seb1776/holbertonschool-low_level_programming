#include "holberton.h"
/**
 * _strlen - Count the total length of a string
 * @s: given string
 *
 * Return: 0
 */
int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{ }

	return (i);
}
